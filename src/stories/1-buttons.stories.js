import React from 'react'
import { action } from '@storybook/addon-actions'
import { IonButton } from '@ionic/react'

export default {
  title: 'Buttons',
}

export const RegularButton = () => <IonButton>Clear</IonButton>
